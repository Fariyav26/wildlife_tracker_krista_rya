<p id="notice"><%= notice %></p>

<h1>Listing Animals</h1>
<div class="container-fluid">
  <div class="row">
    <div class="col-md-3 col-sm-5">
      <div class="input_box">
        <strong><h4>Create New Animal</h4></strong>
        <lable>Common Name:</lable><br>
        <input id="animal_common_name" type="text" name="animal[common_name]" placeholder="Common name" /><br><br>
        <lable>Latin Name:</lable><br>
        <input id="animal_latin_name" type="text" name="animal[latin_name]" placeholder="Latin name" /><br><br>
        <lable>Kingdom:</lable><br>
        <input id="animal_kingdom" type="text" name="animal[kingdom]" placeholder="Kingdom" /><br><br>
        <button id="create_animal" class="btn btn-info btn-sm">Create Animal</button><br><br>
      </div>
    </div>

    <div class="col-md-9 col-sm-5">
      <table id="animal_list" class="table">
        <thead>
          <tr>
            <th>Common name</th>
            <th>Latin name</th>
            <th>Kingdom</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @animals.each do |animal| %>
            <tr>
              <td><%= link_to animal.common_name, animal %></td>
              <td><%= animal.latin_name %></td>
              <td><%= animal.kingdom %></td>
              <!-- <td><%= link_to 'Show', animal %></td> -->
              <td><%= link_to 'Edit', edit_animal_path(animal) %></td>
              <td><%= link_to 'Destroy', animal, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </row>
</div>

<br>

<%= link_to 'New Animal', new_animal_path %> | <%= link_to 'Regions', regions_path %> | <%= link_to 'Sightings', sightings_path %>
